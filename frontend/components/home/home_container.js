import {connect} from 'react-redux';
import Home from './home'

const mSTP = (state)=>{
    return {

    };
};


const mDTP = (dispatch)=>{
    return{

    };
};

export default connect(mSTP, mDTP)(Home);